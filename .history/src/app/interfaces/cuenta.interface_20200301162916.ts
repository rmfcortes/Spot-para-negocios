export interface Cuenta {
    nombre: string,
    precio: number,
    productos: number,
    subCategorias: number,
    pedidos: boolean,
    ofertas: boolean,
    populares: boolean,
    vendidos: boolean,
    actual: boolean,
}
